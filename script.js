let userScore = 0;let compScore = 0;const choices = document.querySelectorAll('.choice')const resultBar = document.querySelector('#result')const genCompChoice = () => {	const compChoices = ['rock', 'paper', 'scissor']	const idx = (Math.random() * 3);	return compChoices[Math.floor(idx)];}const gameDraw = () => {	resultBar.innerText = 'Draw Try Again'}const winner = (userWin, compChoice, userChoice) => {	const userScoreElm = document.querySelector('#user-score')	const compScoreElm = document.querySelector('#comp-score')	if (userWin) {		userScore++;		resultBar.innerText = 'You win your choice: ' + userChoice + " and computer's choice: " + compChoice		userScoreElm.innerText = userScore	}	else {		compScore++;		resultBar.innerText = 'You lose your choice: ' + userChoice + " and computer's choice: " + compChoice		compScoreElm.innerText = compScore	}}const playGame = (userChoice) => {  const compChoice = genCompChoice();	if (userChoice === compChoice){		gameDraw();	} else {		if(userChoice === 'rock') {			userWin = compChoice === 'scissor' ? true : false		} else if(userChoice === 'scissor') {			userWin = compChoice === 'paper' ? true : false		} else {			userWin = compChoice === 'scissor' ? true : false		}		winner(userWin, compChoice, userChoice);	}}choices.forEach( (choice) => {  choice.addEventListener('click', () => {   	const userChoice = choice.getAttribute('id');   	playGame(userChoice);  });});